---
layout: layouts/base.njk
pagination:
  data: medium.posts
  size: 1
  alias: item
permalink: /{{ item.path }}/index.html
---
<div class="date-title">{{ item.pubDate | date }} вiд {{ item.category }}</div><h1>{{ item.title }}</h1><div class="content">{{ item['content:encoded'] | safe }}</div>
